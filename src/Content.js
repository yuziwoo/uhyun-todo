/* eslint-disable */
import { useEffect, useState } from 'react';
import axios from 'axios';
import './Content.css';


function Header() {
  // let firstList = axios.get('');
  let [list, setList] = useState();
  useEffect(() => {
    
  }, [])
  return (
    <main>

    </main>
  );
}

export default Header;